# Claude Code Instructions

Follow `AIDONE.md` before reporting coding work as complete.

For normal product work, apply P0 and P1. Apply relevant P2 checks for production risk, data migration, external services, background jobs, auth, payments, or user data.

Completion claims require evidence. Run the relevant checks for this project, such as tests, lint, type check, build, API smoke tests, browser checks, or manual verification.

If a check cannot be run, state why and provide the closest available verification.
